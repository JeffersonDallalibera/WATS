# Configuração de Gravação WATS - Exemplo
# Adicione essas configurações ao seu arquivo .env para configurar a gravação de sessões

# Configurações de Banco de Dados (existente)
DB_TYPE=postgresql
DB_SERVER=seu_servidor
DB_DATABASE=seu_banco_de_dados
DB_UID=seu_usuario
DB_PWD=sua_senha
DB_PORT=5432

# Configurações de Gravação de Sessão
# Habilitar/desabilitar funcionalidade de gravação
RECORDING_ENABLED=true

# Iniciar gravação automaticamente quando uma conexão é feita
RECORDING_AUTO_START=true

# Modo de gravação: full_screen, rdp_window, active_window
# - full_screen: Grava toda a área de trabalho (todos os monitores)
# - rdp_window: Grava apenas a janela de conexão RDP (recomendado)
# - active_window: Grava a janela atualmente ativa
RECORDING_MODE=rdp_window

# Diretório para armazenar gravações (padrão: diretório de dados do usuário/recordings)
# RECORDING_OUTPUT_DIR=C:/Users/SeuUsuario/AppData/Roaming/WATS/recordings

# Configurações de qualidade de gravação
RECORDING_FPS=10                        # Quadros por segundo (1-60)
RECORDING_QUALITY=23                    # Qualidade H.264 (0-51, menor é melhor)
RECORDING_RESOLUTION_SCALE=1.0          # Escala de resolução (0.1-2.0, 1.0 = completa)

# Configurações de rotação de arquivos
RECORDING_MAX_FILE_SIZE_MB=100          # Tamanho máximo do arquivo antes da rotação (MB)
RECORDING_MAX_DURATION_MINUTES=30      # Duração máxima antes da rotação (minutos)

# Gerenciamento de armazenamento
RECORDING_MAX_TOTAL_SIZE_GB=10.0        # Armazenamento total máximo para gravações (GB)
RECORDING_MAX_FILE_AGE_DAYS=30          # Excluir gravações mais antigas que X dias
RECORDING_CLEANUP_INTERVAL_HOURS=6      # Frequência de limpeza (horas)

# Nível de log
LOG_LEVEL=INFO