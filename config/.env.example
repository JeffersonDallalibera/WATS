
# WATS - Configurações de Ambiente (SQL Server)
# Copie este arquivo para .env e configure com seus valores

# === AMBIENTE ===
WATS_ENV=development
# Opções: development, testing, staging, production

# === MODO DEMO ===
WATS_DEMO_MODE=false
# true = usa dados mock sem conexão com banco
# false = conecta ao SQL Server real

# === BANCO DE DADOS (PRODUÇÃO) ===
DB_HOST=localhost
DB_PORT=1433
DB_NAME=WATS_Production
DB_USER=wats_user
DB_PASSWORD=sua_senha_segura_aqui

# === BANCO DE DADOS (STAGING) ===
STAGING_DB_HOST=staging-server.domain.com
STAGING_DB_PORT=1433
STAGING_DB_NAME=WATS_Staging
STAGING_DB_USER=wats_staging
STAGING_DB_PASSWORD=senha_staging

# === GRAVAÇÃO ===
RECORDING_PATH=C:\WATS\Recordings
RECORDING_ENABLED=true
RECORDING_MAX_SIZE_MB=200
RECORDING_FPS=15
RECORDING_QUALITY=85

# === API INTEGRAÇÃO ===
API_ENABLED=false
API_BASE_URL=https://api.empresa.com/wats
API_KEY=sua_api_key_aqui
API_TIMEOUT=30

# === CONFIGURAÇÕES ADICIONAIS ===
LOG_LEVEL=INFO
# Opções: DEBUG, INFO, WARNING, ERROR, CRITICAL

UI_THEME=system
# Opções: system, light, dark

# === SQL SERVER ESPECÍFICO ===
SQL_DRIVER=ODBC Driver 17 for SQL Server
# Ou: ODBC Driver 18 for SQL Server, SQL Server, etc.

SQL_TRUST_CERTIFICATE=true
# true para desenvolvimento local, false para produção com SSL válido

SQL_ENCRYPT=false
# true para produção, false para desenvolvimento local

SQL_MARS_CONNECTION=true
# Multiple Active Result Sets - geralmente true

# === AUTENTICAÇÃO SQL SERVER ===
# Para usar autenticação do Windows, deixe DB_USER e DB_PASSWORD vazios
# e configure SQL_INTEGRATED_SECURITY=true
SQL_INTEGRATED_SECURITY=false

# === CONNECTION STRING PERSONALIZADA (OPCIONAL) ===
# Se preferir usar connection string completa ao invés dos parâmetros separados
# DB_CONNECTION_STRING=Driver={ODBC Driver 17 for SQL Server};Server=localhost,1433;Database=WATS;UID=sa;PWD=password;TrustServerCertificate=yes;

# === COMPATIBILIDADE COM CÓDIGO EXISTENTE ===
DB_SERVER=localhost
DB_PORT=1433
DB_DATABASE=WATS_Dev
DB_UID=sa
DB_PWD=DevPassword123!
DB_TYPE=sqlserver

# === EXEMPLO DE DEVELOPMENT ===
# DB_HOST=localhost
# DB_PORT=1433  
# DB_NAME=WATS_Dev
# DB_USER=sa
# DB_PASSWORD=DevPassword123!

# === EXEMPLO DE PRODUÇÃO ===
# DB_HOST=sql-server-prod.empresa.com
# DB_PORT=1433
# DB_NAME=WATS_Production
# DB_USER=wats_prod_user
# DB_PASSWORD=SenhaSeguraProducao2024!